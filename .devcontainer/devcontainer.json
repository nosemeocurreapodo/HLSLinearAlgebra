// For format details, see https://aka.ms/devcontainer.json. For config options, see the
    // README at: https://github.com/devcontainers/templates/tree/main/src/cpp
    {
        //"name": "Challenge devcontainer",
        //"build": {
        //	"dockerfile": "../docker/Dockerfile",
        //	"context": ".."
        //},
        //"image": "bcc003ce2b6d/vivado_2023.2:v1",
        "image": "ubuntu:22.04",
        "mounts": [
          "source=/tools/,target=/tools/,type=bind,consistency=cached"
        ],
        "customizations":
            {
              "vscode": {
                "settings": {
                  "terminal.integrated.shell.linux": "/bin/bash"
                },
                "extensions": [
                  "ms-vscode.cpptools-extension-pack",
                  "donjayamanne.python-extension-pack",
                  "google.geminicodeassist",
                  "google.gemini-cli-vscode-ide-companion"
                ]
              }
            },
        //"postCreateCommand": "cp /workspaces/HLSLinearAlgebra/.devcontainer/.settings64-Vitis.sh /tools/Xilinx/Vitis/2023.2/ && apt-get update && apt-get install -y build-essential cmake libeigen3-dev"
        "postCreateCommand": "apt-get update && apt-get install -y build-essential cmake git libeigen3-dev"
        //"postCreateCommand": "cmake -S . -B build && cmake --build build",
        //"remoteUser": "vscode"
  
        // Features to add to the dev container. More info: https://containers.dev/features.
        // "features": {},
  
        // Use 'forwardPorts' to make a list of ports inside the container available locally.
        // "forwardPorts": [],
  
        // Use 'postCreateCommand' to run commands after the container is created.
        // "postCreateCommand": "gcc -v",
  
        // Configure tool-specific properties.
        // "customizations": {},
  
        // Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
        // "remoteUser": "root"
      }
  